<%= form_for @license, :html => { class: 'form-horizontal' } do |f| %>
  <%= render 'layouts/error_messages', target: @license %>
  <div class="control-group">
    <%= f.label :quantity, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :quantity, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :start_date, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :start_date %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :end_date, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :end_date %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :license_status_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.collection_select(:license_status_id, AgreementStatus.all, :id, :name) %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :license_type_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.collection_select(:license_type_id, LicenseType.all, :id, :name) %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                licenses_path, :class => 'btn' %>
  </div>
<% end %>
