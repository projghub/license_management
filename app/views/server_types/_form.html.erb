<%= form_for @server_type, :html => { :class => 'form-horizontal' } do |f| %>
  <%= render 'layouts/error_messages', target: @server_type %>
  <div class="control-server_type">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                server_types_path, :class => 'btn' %>
  </div>
<% end %>
