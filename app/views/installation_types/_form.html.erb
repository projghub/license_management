<%= form_for @installation_type, :html => { :class => 'form-horizontal' } do |f| %>
  <%= render 'layouts/error_messages', target: @installation_type %>
  <div class="control-installation_type">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                installation_types_path, :class => 'btn' %>
  </div>
<% end %>
